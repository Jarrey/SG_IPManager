# HTTP service port (default: 8080)
HTTP_PORT=8080

# Optional HTTPS port (requires SSL_CERT_PATH and SSL_KEY_PATH)
# HTTPS_PORT=8443
# SSL_CERT_PATH=/etc/nginx/ssl/server.crt
# SSL_KEY_PATH=/etc/nginx/ssl/server.key

# Optional timezone for the container, e.g. "Asia/Shanghai"
# TZ=Asia/Shanghai
